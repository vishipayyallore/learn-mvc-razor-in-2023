@{
    ViewData["Title"] = "Delete Amenity";
}

@model AmenityVM

<div class="w-100 card border-0 p-4">
    <div class="card-header bg-primary bg-gradient ml-0 py-0">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-0">Delete Amenity</h2>
            </div>
        </div>
    </div>

    <div class="card-body border p-4">
        <form method="post" class="row" enctype="multipart/form-data">
            <input asp-for="Amenity!.Villa_Number" hidden />
            <div class="p-3">
                <div asp-validation-summary="ModelOnly"></div>
                <div class="form-floating py-1 col-12">
                    <select disabled asp-for="@Model.Amenity!.VillaId" asp-items="@Model.VillaList" class="form-select border shadow">
                        <option disabled selected>--Select Villa--</option>
                    </select>
                    <label asp-for="Amenity!.VillaId" class="ms-2"></label>
                    <span asp-validation-for="Amenity!.VillaId" class="text-danger"></span>
                </div>
                <div class="form-floating py-1 col-12">
                    <input disabled asp-for="Amenity!.Villa_Number" class="form-control border shadow" />
                    <label asp-for="Amenity!.Villa_Number" class="ms-2"></label>
                    <span asp-validation-for="Amenity!.Villa_Number" class="text-danger"></span>
                </div>
                <div class="form-floating py-1 col-12">
                    <input disabled asp-for="Amenity!.SpecialDetails" class="form-control border shadow" />
                    <label asp-for="Amenity!.SpecialDetails" class="ms-2"></label>
                    <span asp-validation-for="Amenity!.SpecialDetails" class="text-danger"></span>
                </div>
                <div class="row pt-2">
                    <div class="col-6 col-md-3">
                        <button type="submit" class="btn btn-danger w-100">
                            <i class="bi bi-check-circle"></i> Delete
                        </button>
                    </div>
                    <div class="col-6 col-md-3">
                        <a asp-controller="Amenity" asp-action="Index" class="btn btn-secondary w-100">
                            <i class="bi bi-x-circle"></i> Cancel
                        </a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}